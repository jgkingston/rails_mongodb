= @user.email

/ %div.username{ajax_path: bgg_api_request_user_path(@user), bgg_username: "#{@user.bgg_username}"}

/ - if @user == current_user
/   = link_to "Edit", edit_user_path(@user)

/ %br
/ Games List:
/ %ul.gamelist
%h2 Gardens
%ul
  - @user.gardens.each do |garden|
    %li
      = link_to garden.name, user_garden_path(@user, garden)
  
%h2 Repos
%div.repolist{ajax_path: git_api_repos_user_path}
  / %ul
  /   - @repos.each do |repo|
  /     %li
  /       = link_to repo["name"], user_gardens_path(current_user, garden: {name: "#{repo['name']}"}), method: :post