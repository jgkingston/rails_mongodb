$('.forest').html("")
$(".forest").append("<%= escape_javascript(render('gardens/grid', user: @user)) %>")

$(document).ready(function(){
        var $container = $('.item-container')

        $container.isotope({
            itemSelector: '.item',
            layoutMode: 'fitRows'   
        }).isotopeSearchFilter();
    })


